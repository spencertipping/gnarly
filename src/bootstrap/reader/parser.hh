#ifndef BOOTSTRAP_READER_PARSER_HH
#define BOOTSTRAP_READER_PARSER_HH

#include <string>
#include <istream>

#include "bootstrap/core/definitions.hh"

BEGIN_NAMESPACE()

namespace internal {

using namespace std;

static gnarly_ref read (istream &in) {
  char       c;
  gnarly_ref value;

  

  while (in >> c && c != ')')
}

}

class $asterisk$lt$lt : public gnarly_value {
public:
  $asterisk$lt$lt () {}
  virtual ~$asterisk$lt$lt () {}

  
};

END_NAMESPACE()

#endif
